import { ReactNode } from 'react';
interface ThemeProviderProps {
  className?: string;
  children?: ReactNode;
}
export declare function ThemeProvider({ className, children }: ThemeProviderProps): JSX.Element;
export {};
