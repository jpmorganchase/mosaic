# Mosaic

## Build mosaic

`yarn build`

## CLI

### Build Static Site

`yarn mosaic build --out ./build --config ./pd.config.js`

### Serve

`yarn mosaic serve --port 8080 --config ./pd.config.js`

## Example

`yarn example`
